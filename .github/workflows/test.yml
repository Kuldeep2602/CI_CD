# name: Deploy to EC2

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     name: Push to EC2
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout the code
#         uses: actions/checkout@v2
#       - name: Execute remote SSH commands using SSH key
#         uses: appleboy/ssh-action@v1
#         with:
#           host: 13.48.59.228
#           username: linuxserver.io
#           key: ${{ secrets.PRIVATE_SSH_KEY }}
#           port: 22
#           script: whoami
name: Deploy to EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Push to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
        
      - name: Execute remote SSH commands using SSH key
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 13.48.59.228
          username: ubuntu  # Changed from linuxserver.io to ubuntu
          key: ${{ secrets.PRIVATE_SSH_KEY }}
          port: 22
          script: |
            echo "Connected successfully!"
            whoami
            pwd